# 專案規則

## 語言
- 所有回覆一律使用**繁體中文**。

## 檔案修改範圍
- 除非使用者明確下達「掃描整個專案」的指令，否則修改範圍限定於：
  - 程式檔案（如 `.ts`、`.tsx`、`.js`、`.py` 等）
  - Markdown 文件（`.md`）
- 不得主動讀取或修改範圍以外的檔案。

## Token 消耗控管
- 執行前預估會大量消耗 token 的動作（例如：全專案掃描、大量檔案讀取、呼叫外部 API 多次等），**必須先詢問使用者確認後才能執行**。
- 若不確定某動作的 token 消耗量，保守估計，先問再做。
